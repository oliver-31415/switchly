org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Optional Firebase/Crashlytics plugin application for local/private builds.
# Set to true in ~/.gradle/gradle.properties or a local uncommitted gradle.properties.
switchly.firebase=true
