<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Google Play Billing for Play Store Premium purchases -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- Network access for billing, maps, update checks, and optional online resources -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Usage access for screen-time statistics and app blocking decisions -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <!-- Direct system prompt for excluding Switchly from battery optimization -->
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
        tools:ignore="BatteryLife" />

    <!-- Network-state checks for online features and reliability guidance -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Wi-Fi state access for Wi-Fi schedules and triggers -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Android 13+ nearby Wi-Fi access without location derivation -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="33" />

    <!-- Legacy Bluetooth permissions for Android 11 and lower -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Android 12+ Bluetooth connection access -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />

    <!-- Location access needed to read Wi-Fi SSID and evaluate location schedules -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Exact alarms for schedules on Android 12+ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Background location improves Wi‑Fi trigger reliability when the app is not in the foreground.
         We request it in a 2‑step flow: first "While using the app", then guide the user to "Allow all the time". -->
    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:targetApi="29" />

    <!-- Restart blockers, schedules, and widgets after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- NFC tag reading and writing -->
    <uses-permission android:name="android.permission.NFC" />

    <!-- Notification runtime permission on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground services for blocking and trigger monitoring -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Android 14+ permission for Bluetooth connected-device foreground service type -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"
        tools:targetApi="34" />

    <!-- Background-process cleanup is only effective for other apps through Android 13. -->
    <uses-permission
        android:name="android.permission.KILL_BACKGROUND_PROCESSES"
        android:maxSdkVersion="33" />

    <!-- Camera access for QR and barcode scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera hardware is optional so Switchly can install on devices without a camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- NFC hardware is optional so Switchly can install on devices without NFC -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <!-- Package visibility used by app pickers and protected-app detection. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.view.InputMethod" />
        </intent>
        <intent>
            <action android:name="android.settings.SETTINGS" />
        </intent>
    </queries>

    <application
        android:name=".app.SwitchlyApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Switchly"
        android:supportsRtl="true"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Device-admin receiver for Advanced Mode/uninstall protection -->
        <receiver
            android:name=".receiver.DPMReceiver"
            android:exported="true"
            android:label="@string/advanced_mode_admin_label"
            android:description="@string/advanced_mode_admin_description"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/switchly_device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Main launcher activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Transparent entry point for QR, barcode, NFC-write, and blocked-notification shortcuts -->
        <activity
            android:name=".feature.entry.ScanLauncherActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Settings root screen -->
        <activity
            android:name=".feature.settings.SettingsActivity"
            android:exported="false"
            android:label="@string/settings" />

        <!-- Rules hub screen -->
        <activity
            android:name=".feature.tools.RulesHubActivity"
            android:exported="false"
            android:label="@string/nav_rules" />

        <!-- Activity hub screen -->
        <activity
            android:name=".feature.tools.ActivityHubActivity"
            android:exported="false"
            android:label="@string/nav_activity" />

        <!-- Schedule management screen -->
        <activity
            android:name=".feature.schedule.SchedulesActivity"
            android:exported="false"
            android:label="@string/schedules_title"
            android:parentActivityName=".feature.settings.SettingsActivity" />

        <!-- Map picker for location-based schedules -->
        <activity
            android:name=".feature.schedule.LocationMapPickerActivity"
            android:exported="false"
            android:label="@string/schedules_location_map_picker_title"
            android:parentActivityName=".feature.schedule.SchedulesActivity" />

        <!-- First-run onboarding flow -->
        <activity
            android:name=".feature.onboarding.OnboardingActivity"
            android:exported="false" />

        <!-- Profile management screen -->
        <activity
            android:name=".feature.profiles.ManageProfilesActivity"
            android:exported="false" />

        <!-- App rules picker screen -->
        <activity
            android:name=".feature.picker.AppPickerActivity"
            android:exported="false" />

        <!-- Key/code management hub -->
        <activity
            android:name=".feature.tools.ManageKeysActivity"
            android:exported="false" />

        <!-- NFC action tag writer -->
        <activity
            android:name=".nfc.NfcWriterActivity"
            android:exported="false" />

        <!-- NFC write waiting screen -->
        <activity
            android:name=".nfc.NfcWriteWaitingActivity"
            android:exported="false" />

        <!-- NFC dispatch activity for Switchly NDEF and fallback tag scans -->
        <activity
            android:name=".nfc.NfcEntryActivity"
            android:exported="true"
            android:launchMode="singleTop">

            <!-- Switchly action and legacy URI tags. Host validation happens in code. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="switchly" />
            </intent-filter>

            <!-- Paired UID fallback for writable tags that contain unrelated plain text. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Tech-discovered fallback for cards/tags that are claimed earlier than TAG_DISCOVERED for example IsoDep bank cards and some external/read-only tags. -->
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />

            <!-- Fallback for blank/non-NDEF tags so Switchly can still react. -->
            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Fullscreen blocker shown when a blocked app or website is opened -->
        <activity
            android:name=".feature.blocker.BlockerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:theme="@style/Theme.Switchly.Blocker"
            tools:ignore="DiscouragedApi" />

        <!-- Support log viewer -->
        <activity
            android:name=".feature.support.SupportLogActivity"
            android:exported="false"
            android:label="@string/support_logs_view_title" />

        <!-- Advanced mode information screen -->
        <activity
            android:name=".feature.about.AdvancedModeActivity"
            android:exported="false"
            android:label="@string/advanced_mode_title" />

        <!-- FAQ screen -->
        <activity
            android:name=".feature.faq.FaqActivity"
            android:exported="false"
            android:label="@string/faq_screen_title" />

        <!-- Premium information screen -->
        <activity
            android:name=".feature.premium.PremiumInfoActivity"
            android:exported="false"
            android:label="@string/premium_title" />

        <!-- Premium redeem-code screen -->
        <activity
            android:name=".feature.premium.PremiumRedeemActivity"
            android:exported="false"
            android:label="@string/premium_redeem_title"
            android:parentActivityName=".feature.premium.PremiumInfoActivity" />

        <!-- Permission setup screen -->
        <activity
            android:name=".feature.settings.PermissionsActivity"
            android:exported="false"
            android:parentActivityName=".feature.settings.SettingsActivity" />

        <!-- Home display/toggle options screen -->
        <activity
            android:name=".feature.settings.ToggleOptionsActivity"
            android:exported="false" />

        <!-- Blocking mode settings screen -->
        <activity
            android:name=".feature.settings.BlockingModesActivity"
            android:exported="false"
            android:parentActivityName=".feature.settings.SettingsActivity" />

        <!-- Blocking feature settings screen -->
        <activity
            android:name=".feature.settings.BlockingFeaturesActivity"
            android:exported="false"
            android:parentActivityName=".feature.settings.SettingsActivity" />

        <!-- Blocked notification inbox -->
        <activity
            android:name=".feature.inbox.BlockedInboxActivity"
            android:exported="false"
            android:label="@string/blocked_inbox_title"
            android:parentActivityName=".feature.settings.SettingsActivity" />

        <!-- External QR/deep-link confirmation entry point -->
        <activity
            android:name=".feature.qr.ExternalQrActionActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="switchly" />
            </intent-filter>
        </activity>

        <!-- QR scanner screen -->
        <activity
            android:name=".feature.qr.QrScanActivity"
            android:exported="false" />

        <!-- Barcode scanner screen -->
        <activity
            android:name=".feature.barcode.BarcodeScanActivity"
            android:exported="false" />

        <!-- QR action generator screen -->
        <activity
            android:name=".feature.qr.QrGenerateActivity"
            android:exported="false" />

        <!-- What's new/release notes screen -->
        <activity
            android:name=".feature.about.WhatsNewActivity"
            android:exported="false"
            android:label="@string/settings_changelog_title"
            android:parentActivityName=".feature.settings.SettingsActivity" />

        <!-- Support screen -->
        <activity
            android:name=".feature.support.SupportActivity"
            android:exported="false"
            android:label="@string/support_title"
            android:parentActivityName=".feature.settings.SettingsActivity" />

        <!-- Switchly action and input overview -->
        <activity
            android:name=".feature.usage.SwitchlyOverviewActivity"
            android:exported="false" />

        <!-- Active time overview -->
        <activity
            android:name=".feature.usage.ActiveTimeActivity"
            android:exported="false" />

        <!-- Active time detail screen for sessions and monthly totals -->
        <activity
            android:name=".feature.usage.ActiveTimeDetailActivity"
            android:exported="false" />

        <!-- Recent Switchly activity timeline -->
        <activity
            android:name=".feature.usage.ActivityHistoryActivity"
            android:exported="false" />

        <!-- App and website usage overview -->
        <activity
            android:name=".feature.usage.AppWebsiteUsageActivity"
            android:exported="false" />

        <!-- App usage detail screen -->
        <activity
            android:name=".feature.usage.AppUsageDetailActivity"
            android:exported="false" />

        <!-- App launch statistics and timeline screens -->
        <activity
            android:name=".feature.usage.AppLaunchesActivity"
            android:exported="false" />

        <!-- Apps excluded from usage statistics -->
        <activity
            android:name=".feature.usage.IgnoredUsageAppsActivity"
            android:exported="false" />

        <activity
            android:name=".feature.usage.AppLaunchDetailActivity"
            android:exported="false" />

        <!-- Screen unlock statistics and detail timeline screens -->
        <activity
            android:name=".feature.usage.ScreenUnlocksActivity"
            android:exported="false" />

        <activity
            android:name=".feature.usage.ScreenUnlockDetailActivity"
            android:exported="false" />

        <!-- Website usage detail screen -->
        <activity
            android:name=".feature.usage.WebsiteUsageDetailActivity"
            android:exported="false" />

        <!-- Website rules management screen -->
        <activity
            android:name=".feature.settings.ManageBlockedWebsitesActivity"
            android:exported="false" />

        <!-- In-app rules setup screen -->
        <activity
            android:name=".feature.settings.InAppRulesActivity"
            android:exported="false" />

        <!-- Paired NFC tag management screen -->
        <activity
            android:name=".feature.settings.ManagePairedTagsActivity"
            android:exported="false" />

        <!-- Barcode action management screen -->
        <activity
            android:name=".feature.settings.ManageBarcodesActivity"
            android:exported="false" />

        <!-- App lock settings screen -->
        <activity
            android:name=".feature.settings.AppLockSettingsActivity"
            android:exported="false" />

        <!-- App lock challenge screen -->
        <activity
            android:name=".feature.settings.AppLockActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop" />

        <!-- App information screen -->
        <activity
            android:name=".feature.about.AppInfoActivity"
            android:exported="false" />

        <!-- Device information screen -->
        <activity
            android:name=".feature.about.DeviceInfoActivity"
            android:exported="false" />

        <!-- Other Switchly products screen -->
        <activity
            android:name=".feature.about.OtherSwitchlyProductsActivity"
            android:exported="false" />

        <!-- Developer information screen -->
        <activity
            android:name=".feature.about.DeveloperInfoActivity"
            android:exported="false" />

        <activity
            android:name=".feature.about.PrivacyReportActivity"
            android:exported="false" />

        <!-- Package-added receiver to refresh app lists after installs -->
        <receiver
            android:name=".platform.receiver.apps.PackageAddedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Post-update receiver to re-register watchers after app updates -->
        <receiver
            android:name=".platform.receiver.system.PostUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver to restart blockers, schedules, and widgets -->
        <receiver
            android:name=".platform.receiver.system.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Time-change receiver to re-evaluate schedules -->
        <receiver
            android:name=".platform.receiver.system.TimeChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Exact-alarm permission receiver to reschedule alarms after grant changes -->
        <receiver
            android:name=".platform.receiver.system.ExactAlarmPermissionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- User-unlock receiver to re-evaluate schedules and blockers -->
        <receiver
            android:name=".platform.receiver.system.UserUnlockReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

        <!-- Geofence receiver for location-based schedules -->
        <receiver
            android:name=".platform.receiver.location.LocationGeofenceReceiver"
            android:exported="false" />

        <!-- Schedule receiver triggered by explicit PendingIntents -->
        <receiver
            android:name=".platform.receiver.schedule.ScheduleReceiver"
            android:exported="false" />

        <!-- Quick Settings tile for toggling Switchly -->
        <service
            android:name=".platform.tile.SwitchlyTileService"
            android:exported="true"
            android:icon="@drawable/qs_switchly_24"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Quick Settings tile for opening the QR scanner -->
        <service
            android:name=".platform.tile.QrScanTileService"
            android:exported="true"
            android:icon="@drawable/qs_qr_24"
            android:label="@string/qr_scan_title"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Quick Settings tile for opening the barcode scanner -->
        <service
            android:name=".platform.tile.BarcodeScanTileService"
            android:exported="true"
            android:icon="@drawable/qs_barcode_24"
            android:label="@string/barcode_scan_title"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Shared receiver for home-screen widget quick actions -->
        <receiver
            android:name=".widget.QuickActionReceiver"
            android:exported="false" />

        <!-- Home-screen widget for scanner shortcuts -->
        <receiver
            android:name=".widget.ScannerWidgetProvider"
            android:exported="true"
            android:label="@string/widget_scanner_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/scanner_widget_info" />
        </receiver>

        <!-- Home-screen widget for QR scanning -->
        <receiver
            android:name=".widget.QrScanWidgetProvider"
            android:exported="true"
            android:label="@string/widget_qr_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/qr_scan_widget_info" />
        </receiver>

        <!-- Home-screen widget for barcode scanning -->
        <receiver
            android:name=".widget.BarcodeScanWidgetProvider"
            android:exported="true"
            android:label="@string/widget_barcode_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/barcode_scan_widget_info" />
        </receiver>

        <!-- Home-screen widget for NFC writing -->
        <receiver
            android:name=".widget.NfcWriteWidgetProvider"
            android:exported="true"
            android:label="@string/widget_nfc_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nfc_write_widget_info" />
        </receiver>

        <!-- Home-screen widget for focus-now/temporary mode -->
        <receiver
            android:name=".widget.FocusNowWidgetProvider"
            android:exported="true"
            android:label="@string/widget_focus_now_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/focus_now_widget_info" />
        </receiver>

        <!-- Home-screen widget for blocked notifications -->
        <receiver
            android:name=".widget.BlockedNotificationsWidgetProvider"
            android:exported="true"
            android:label="@string/widget_blocked_notifications_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/blocked_notifications_widget_info" />
        </receiver>

        <!-- Home-screen widget for pausing the blocker -->
        <receiver
            android:name=".widget.PauseBlockerWidgetProvider"
            android:exported="true"
            android:label="@string/widget_pause_blocker_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/pause_blocker_widget_info" />
        </receiver>

        <!-- Home-screen widget for the next scheduled action -->
        <receiver
            android:name=".widget.NextScheduleWidgetProvider"
            android:exported="true"
            android:label="@string/widget_next_schedule_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="at.saltyy.switchly.schedule.NEXT_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_schedule_widget_info" />
        </receiver>

        <!-- Home-screen widget showing the current active Switchly timer -->
        <receiver
            android:name=".feature.widget.ActiveTimerWidgetProvider"
            android:exported="true"
            android:label="@string/widget_active_timer_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_active_timer_info" />
        </receiver>

        <!-- Accessibility service used for app and website blocking -->
        <service
            android:name=".blocking.SwitchlyAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/switchly_accessibility_config" />
        </service>

        <!-- Notification listener service used to hide notifications from blocked apps -->
        <service
            android:name=".blocking.NotificationBlockerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Foreground service for Wi-Fi based profile triggers -->
        <service
            android:name=".platform.receiver.wifi.WifiTriggerService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Foreground service for Bluetooth based profile triggers -->
        <service
            android:name=".platform.receiver.bluetooth.BluetoothTriggerService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Bluetooth connection receiver for profile trigger updates -->
        <receiver
            android:name=".platform.receiver.bluetooth.BluetoothConnectionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
                <action android:name="android.bluetooth.a2dp.profile.action.CONNECTION_STATE_CHANGED" />
                <action android:name="android.bluetooth.headset.profile.action.CONNECTION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing exported backups, logs, and support files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
